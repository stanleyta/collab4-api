# py-langgraph

A minimal LangGraph example with support for Gemini, Anthropic, and OpenAI.

## Setup

Using `uv`:

```bash
uv sync
```

## Running

```bash
uv run main.py --provider gemini
```

## Environment Variables

Create a `.env` file based on `.env.example`.
